---
interface Props {
	class?: string;
	id?: string;
}

const { class: className, id } = Astro.props;
---

<section
	id={id}
	data-section={id}
	class={`ds-container ds-section ${className}`}
>
	<slot />
</section>
